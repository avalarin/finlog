{
  "name": "finlog-functions-batch-operations",
  "main": "index.js",
  "scripts": {
    "build": "npm install && npx tsc -p ./tsconfig.json",
    "test": "npx jest"
  },
  "dependencies": {
    "finlog-core": "*"
  }
}
